<resources>
    <style name="Theme.QuickLoan" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Main Palette -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowBackground">@color/background</item>

        <!-- Force dark readable text in all input fields -->
        <item name="editTextColor">@color/text_main</item>
        <item name="android:editTextColor">@color/text_main</item>
        <item name="android:textColor">@color/text_main</item>
        <item name="android:textColorHint">@color/text_muted</item>
    </style>
</resources>
